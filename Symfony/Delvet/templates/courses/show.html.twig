{% extends 'base.html.twig' %}

{% block title %}Courses{% endblock %}

{% block body %}
    <h1>Courses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ course.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ course.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ course.image }}</td>
            </tr>
            <tr>
                <th>Date_create</th>
                <td>{{ course.dateCreate ? course.dateCreate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ course.description }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('courses/_delete_form.html.twig') }}
    <a href="{{ path('courses') }}" class="btn btn-info">back to list</a>

    <a href="{{ path('courses_edit', {'id': course.id}) }}"class="btn btn-success">edit</a>

    </div>

    
{% endblock %}
